name: Test

on:
  push:
    branches-ignore:
      - 'main'
  pull_request:

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
      - uses: actions/setup-go@40f1582b2485089dde7abd97c1529aa768e1baff # v5
        with:
          go-version-file: .go-version
          cache: true
      - name: Run tests
        run: go test ./...
      - name: Validate release tag graphs
        run: ruby .github/scripts/check-release-tag-graphs.rb

  desktop-pinned-pi-smoke:
    name: Desktop pinned Pi smoke
    runs-on: macos-14
    defaults:
      run:
        shell: bash
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          fetch-depth: 0
      - uses: actions/setup-go@40f1582b2485089dde7abd97c1529aa768e1baff # v5
        with:
          go-version-file: .go-version
          cache: true
      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: '22.23.1'
          cache: npm
          cache-dependency-path: |
            desktop/package-lock.json
            desktop/runtime/pi/package-lock.json
      - name: Provision and require exact pinned Pi smoke
        run: desktop/scripts/provision-pinned-pi-smoke.sh

  desktop-mac-app:
    name: Desktop macOS app artifact
    runs-on: macos-14
    # A run artifact only: no release, no tag, no publication. Read-only
    # permissions so the token refuses publishing even if a step tried.
    permissions:
      contents: read
    defaults:
      run:
        shell: bash
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          fetch-depth: 0
      - uses: actions/setup-go@40f1582b2485089dde7abd97c1529aa768e1baff # v5
        with:
          go-version-file: .go-version
          cache: true
      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: '22.23.1'
          cache: npm
          cache-dependency-path: |
            desktop/package-lock.json
            desktop/runtime/pi/package-lock.json
      - name: Provision the pinned Pi runtime
        run: desktop/scripts/provision-pinned-pi-smoke.sh
      - name: Package the macOS app
        working-directory: desktop
        run: npm run package:mac
      - name: Archive the bundle with its symlinks intact
        working-directory: desktop
        # The .app carries symlinks in Contents/Frameworks; ditto -c -k stores
        # them as links, and --keepParent keeps the .app wrapper itself.
        run: ditto -c -k --sequesterRsrc --keepParent "release/mac-arm64/Void Code.app" release/void-code-mac-arm64.zip
      - name: Upload the macOS app
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: void-code-mac-arm64
          path: desktop/release/void-code-mac-arm64.zip
          if-no-files-found: error
