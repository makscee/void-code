# Authoritative model-decision RED — R5 correction of a5cefbf

Authority: frozen `client-model-decision-contract-closure-r5.md`, SHA256
`9bf358071bbd70d16395fd2bc012b8e5719afa019588152a643680b1363d3613`;
R5 panel: three NO FINDINGS, GO **test-only RED**. The R4 STOP is NOT_RUN,
not RED. The obsolete Go reducer test is removed; Go owns wire/embed only.

## Boundaries and executable seams

Only the seven R5 §10 owned paths and deletion of `cmd/vc/pi_model_decision_test.go`
may change. No implementation, dependency/pin/lock changes, home state, live endpoint,
inference, push or amend. Existing lockfile dependencies only. Fixtures use the exact
provisioned Pi 0.84.1 and bundled Node v22.23.1, verified against repository pins.
Pin modules load natively; missing dependencies, syntax and top-level import errors
are not caught/reclassified. Erased ABI declarations describe only the pin methods
used by fixtures; they supply no runtime implementations. Constructors and methods
always come from the verified native modules. Resolution prefers this checkout's
staged tree, then the shared worktree artifact; `VC_MODEL_TEST_PI_ROOT` /
`VC_MODEL_TEST_NODE` can locate the same byte-identical runtime elsewhere. A missing
or different pin fails qualification. Missing future product files/exports are ordinary
explicit assertions, not skips and not fixture substitutions.

Future source seam: `cmd/vc/pi_model_decision.ts` exports the pure initializer,
`deriveTransition`, `reduce`, codecs and canonical serializer; `cmd/vc/pi_extension.ts`
is the real default extension factory Go embeds, exporting its managed stream and
read-only controller inspection/control access. Test observers can record envelopes,
publications, effect enqueue/ingress and pause worker/dequeue. They cannot install
state, allocate production tokens, invoke production readback, synthesize completion
or replace successful Pi calls. Pure reducer unit tests may explicitly submit events;
they do not count as adapter witnesses. The production factory is loaded with pinned
jiti/loader and bound to the real runner, AgentSession, ModelRuntime, ModelRegistry
and SessionManager. Time, bootstrap, isolated HTTP responses and exact Pi method
faults are the only injected dependencies.

Canonical digest oracle: `v1:` + SHA256 of UTF-8 canonical JSON of
`{domain, value, version:1}`. Domain is exactly `authority`, `legacy`, or `catalog`.
Authority value is `{outcome, authority}` (generation/evidence are separately bound);
legacy value is the complete seven-field projection; catalog value is the complete
ordered model-definition array. Canonical JSON recursively ASCII-sorts object keys,
uses JSON scalar spelling, preserves array order and permits no undefined, nonfinite
numbers or bigint in wire authority. Tests compare actual digest bytes, not merely
hex shape. No locale sorting, model-ID-only digest or fixture reducer.

### R5 observable header seam

Production captures nine fixed `Response.headers.has/get` slots once each, synchronously,
before external response hooks/body reads: absent, exact combined value, over_limit or
unreadable. Each retained string <=21848 UTF-8 bytes; aggregate <=196632. Empty != absent.
No raw/getAll, occurrence count, trim/split/pick/dedup/rejoin or lower product seam.

HTTP fixture emits physical HTTP/1.1 bytes into an isolated standard Node Duplex
connector consumed by the real pinned Undici Client HTTP parser and Fetch. No TCP/DNS
or global fetch replacement. Fixture records sent bytes only; physical occurrences
never enter product observations. Two fields `fallback` + `fallback` and one field
`fallback, fallback` must give identical nine-name has/get results; both product paths
close empty/readback. Single exact seven-field fallback narrows only. `A,B` can agree
with encoded ordered diagnostics; physical A + B coalesces to `A, B` and rejects.
Whitespace erased by Fetch is not attributed; observable OWS rejects. Controls and
production witnesses are separately named and reported.

## Contract → named test family → mutation map

| R5 contract | Owning file / named family | Mutations to kill after GREEN |
|---|---|---|
| §2.1 V2 opaque authority/decimal wire fidelity, canonical duration validation, compatibility only | Go `TestPiModelDecisionWireBootstrapV2` round-trip of the actual bootstrap type; reducer / bootstrap | omitted descriptor; URL/decimal rewriting; invalid durations; ceiling-as-grant |
| §§2–3 closed codec, pg bigint bounds, no unbounded parse | reducer / generation and closed wire | float/lexical compare; +1 max; unknown/missing accepted |
| §4 evidence != authority; duplicate/stale/newer/conflict | reducer / evidence | receipt-time renewal; max(old,new) deadline; equal eval conflict ignored |
| §§2,4 every authority member incl successor and outcome | reducer / authority one-field matrix | omitted field/revision; order-insensitive list; request-admitted effective substituted |
| §5 invalid greater/conflict/empty safety floor | reducer / quarantine recovery | invalid resets floor; equal/lower recovery; empty treated initial |
| §6 all 9 control/latch tuples and invalid complements | reducer / tuple matrix | derive latch from pct; active→warn clears latch; restricted multi-model |
| §8 ns arithmetic, bounds, exact expiry/delayed timer | reducer / lease boundaries | omit RTT/skew; wrap/saturate; wall clock; > instead of >= |
| §7.1 memory edges, forbidden choice, downgrade, independent sessions | reducer + adapter / memory | capture while closed; overwrite on repeated restriction; consume on stale/failure |
| §§7.2.1–2 explicit observation/retry token envelope | reducer + adapter / envelope | side-channel token; duplicate/skipped ordinal; stale envelope cancels current |
| §7.2.2 deterministic preview; one commit then publish/enqueue | reducer + adapter / two-phase trace | mutable input; worker before publish; preview permission |
| §§7.2.3–7 exact token/null variants, failure/stale/retry | reducer + pinned-registry / exact-token failures | generation-only ACK; stale failure cancels new; failed gate opens |
| §§9–10.3.1 real registry replacement and current object | pinned-registry / native controls and product failure matrix | keep wider registry; missing fresh lookup; no explicit setModel |
| §10.3.1 delegated invalid registration; false; sync throw; pre/post selection rejection; native auth | pinned-registry / independently named method faults | swallow catch; false==success; omit failure event; consume memory before success |
| §§7.2.4–7 own model_select, serialization and races | adapter / worker barriers | deadlock own hook; overlapping worker; stale queued work; uncommitted success opens |
| §§2.3.0,10.2 Fetch binding, HTTP bytes/coalescing | adapter / header fixture controls | wrong native fetch; separator change; value picking; physical input fabricated |
| §§2.3.1–3 exact fallback immediate closed singleton + mandatory readback | adapter / legacy projection | always reject; grant legacy; readback/body before closure; compatible substitute |
| §2.3.2 missing/empty/coalesced/OWS/bounds/unreadable | adapter + reducer / header grammar matrix | trim/split/dedup; truncated generation; merge across responses |
| §2.3.3 state × lower/equal/greater legacy | reducer + adapter / legacy generation matrix | stale narrows; duplicate renews; equal clears quarantine/empty/expiry |
| §2.3.4 encoded and individual atomic precedence | adapter / encoded overlap matrix | diagnostic override; absent==invalid; skip present overlap; malformed pair broadens |
| §2.3.5 header receipt ordering, fresh mandatory GET context | adapter / same-response trace | prior poll satisfies mandatory GET; response hook before snapshot; body before publication |
| §§2.3.3,7.2.6 legacy failures/retry/full completion | pinned-registry + adapter / legacy retry | legacy success opens; retry clears refresh; reuse legacy token for full authority |
| §7.3.2 independent safety fold, latest-only preference | branch-restore / corrupt suffix | latest-only safety; bad record resets floor; invalid preference vetoes safety |
| §7.3 actual active path vs all history | branch-restore / sibling and ordering controls | getEntries; timestamp/ID sort; sibling contamination |
| §7.3.2 equal safety joins, bounds, bindings, path prefix | branch-restore / proof matrix | equal quarantine cleared; unknown-version generation extracted; foreign grants |
| §7.3.3 tree retained floor + branch rebase | branch-restore / navigateTree | epoch unchanged; older branch lowers; old completion opens |
| §7.3.3 real fork/clone label rechaining/resume/reload/new | branch-restore / runtime lifecycle | copy proof trusted; historical deadline; prior runtime injects authority |
| §§7.2.7,9 real managed stream at every pause/failure/restart | adapter + pinned-registry + branch / zero POST | helper-only guard; stale/staged object bypass; cached guard before await |

## Sequence and reporting

1. This plan precedes all test edits.
2. Qualify pin integrity and native behavior controls separately from product RED.
3. Build deterministic table-driven witnesses, explicit nanoseconds and promise barriers;
   no sleeps or live network. Record each uncovered obligation rather than count a name
   or a missing-export assertion as proof its downstream assertions passed.
4. Run targeted Vitest under pinned Node, isolated tests-inclusive typecheck/lint,
   targeted Go wire, repository Go test before commit, and `git diff --check`.
5. Verify every non-owned tracked file byte-identical to a5cefbf; node_modules unstaged.
6. Commit only if the full corrected RED is valid, exactly
   `test(vc): correct authoritative model decision RED`. No amend/push.

Mutation scores and GREEN coverage are **not available on missing implementation**.
The named semantic mutations above are mandatory for the separate implementation lane;
fixture controls must already pass here. Environment/fixture failures block RED qualification.
Final report must distinguish passing controls, ordinary product RED, compilation/import
errors and NOT_RUN obligations; it must never claim complete R5 coverage from test count.

## Verification receipt (test-only correction)

No production/default timing values are chosen. Go tests the actual bootstrap type's
V2 wire fidelity using fixture durations, and exact equality of the source Go embeds
with the TS factory under test. Production Go configuration/default selection is not
inferred from this wire round-trip. Existing V1 expectations in `pi_bootstrap_test.go`
are untouched (outside this ownership); the implementation handoff must account for
that separate migration, not satisfy them by emitting V1 to the new adapter.

Observer convention in the future factory's `modelDecision` test options:
`commit` carries the real envelope, `publish` the real immutable snapshot,
`enqueue-effects` the committed work, `event-ingress` actual product completion/local
selection bodies, and `readback-request` the actual nonsecret URL/cache/controller/epoch
request metadata. Current allocation faults return a `diagnostic` effect with
`stage:adapter_invariant`, `boundedErrorClass:effect_plan_protocol_error`, plus a
`requestReadback` effect and no Pi attempt. No observer installs state or returns
synthetic completions.
Snapshots recorded at method entry are copied immediately and their original frozen
status recorded, so later mutations cannot retroactively manufacture closure evidence.
The native readable-stream reader is observed call-through: producer body emission is
not accepted as evidence of consumer body reading. Initial stream checks invoke the
real export even before provider installation; whenever registered, exact function
identity with the registered managed stream is required. Unplanned model POSTs get a
fixture-only HTTP 599 trap so the real stream drains: the **zero POST counter** must
still fail, rather than a guard mutant merely hanging on an unanswered mock request.
The trap is a transport control, never a product guard or completion event.

### Results

| Run | PASS | ordinary contractual RED | skipped |
|---|---:|---:|---:|
| header/adapter file | 36 controls | 181 missing managed module | 0 |
| real pinned registry file | 5 controls | 32 missing managed module | 0 |
| branch restoration file | 6 controls | 37 missing managed module | 0 |
| pure reducer file | 0 | 240 missing reducer module | 0 |
| **targeted Vitest total** | **47** | **490** | **0** |
| targeted Go wire | 0 | 2 | 0 |
| full `CGO_ENABLED=0 go test -json ./...` | 705 | same 2 wire assertions | 24 |

The 250 managed witnesses stop at the explicit absence of `cmd/vc/pi_extension.ts`;
the 240 reducer witnesses stop at `cmd/vc/pi_model_decision.ts`. Downstream product
assertions are **not executed**, and the counts are not a mutation score or a claim
that Pi failures have already been handled by production. No dependency, syntax,
top-level import or fixture error is relabeled as RED. The fixture-control-only run
passes 47/47 before the full run; its name filter excludes product cases intentionally,
whereas the full targeted run has zero skips.

Strict tests-inclusive TypeScript checking and isolated ESLint: exit 0.
`git diff --check`: exit 0. All **436 non-owned tracked blobs** match their exact
`a5cefbf76cc6ffa35728964f080aaf354a78caad` contents. Only the seven owned paths and
obsolete Go-test deletion differ; node_modules is ignored and unstaged.

Pin controls verify the package/lock/integrity from `resource-pins.json`, full Pi tree
`23c70701cca3e33a2d9139e784487d2f6e3b9dffb8cf87dd45a98c16348c04cc`, actual executing
Node binary `2e3f1286a7eb3736346ed1803e458a0ff909e2b2d5bc746144dcb76970e9b99d`,
Node v22.23.1/native Undici 6.27.0, and actual Pi-installed Fetch/Headers identity with
Undici 8.9.0. Headers source hash is `0ca9bcb1cb174a19311b27f55b954d53e5adc06d6aa935c42df96c9a4453d0a7`.
The project tree-hash function independently confirms the same Pi tree after the runs.
Physical two-field / equivalent single-field observations match. Leading HTTP OWS is
erased by this parser, trailing and embedded OWS are retained; tests distinguish those
facts rather than applying constructor-only Headers normalization to HTTP responses.

Two **fixture-only** selected mutations were executed and reverted:
1. silently drop repeated physical fields in the responder → control fails with
   expected two quota-state lines, received one;
2. remove the native consumer-read observation → body control fails on missing
   `body-byte-consumed`, despite producer emission.
Both die in ordinary assertions (2/2). No production/pinned file was mutated.
Product semantic mutations remain mandatory after the separate implementation exists.

### Reproduction

From this checkout (`N` must be the qualified bundled Node, not PATH Node):

```sh
N=/Users/gever/GIT/maks-startup/void-code/desktop/resources/staged/node/bin/node
cd desktop
FILES=(tests/pi-model-decision-{reducer,adapter,pinned-registry,branch-restore}.test.ts)
"$N" node_modules/vitest/vitest.mjs run "${FILES[@]}" -t 'fixture controls'
"$N" node_modules/vitest/vitest.mjs run "${FILES[@]}"
node_modules/.bin/tsc --strict --noEmit --skipLibCheck --target es2023 \
  --module esnext --moduleResolution bundler --esModuleInterop --types node \
  tests/fixtures/pi-model-decision.ts "${FILES[@]}"
node_modules/.bin/eslint tests/fixtures/pi-model-decision.ts "${FILES[@]}"
cd ..
CGO_ENABLED=0 go test ./cmd/vc -run '^TestPiModelDecisionWire' -count=1
CGO_ENABLED=0 go test -json ./...
git diff --check
```

Tool dependencies were installed with `npm ci --offline --ignore-scripts --no-audit
--no-fund` from the unchanged lock. Full JSON receipts are local, not staged:
`/tmp/vc-model-r5-final.json`, `/tmp/vc-model-r5-controls-final.json`,
`/tmp/vc-model-r5-go-final.jsonl`, and `/tmp/vc-model-r5-mut-*.json`.

This is RED-only authority. No implementation, live endpoint/inference, policy,
publication, user session, release, push or amend is performed.
