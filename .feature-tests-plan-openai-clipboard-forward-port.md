# OpenAI-only fullscreen clipboard forward-port test plan

[INFERRED] User regression: arbitrary Pi fullscreen transcript selection cannot be copied in either terminal `vc` or Void Code desktop after `v0.2.54` OpenAI-only integration superseded the unmerged clipboard branch.

| # | Category | Contract | Source | Priority |
|---|---|---|---|---|
| 1 | Regression | Actual pinned Pi 0.84.1 fullscreen selection + CLI Ctrl+C writes exact Unicode/multiline plain text through native clipboard, not OSC52 | user/manual + prior RED suite | P0 |
| 2 | Regression | macOS desktop Cmd+C with Pi selection emits copy-only intent and writes once; empty selection never becomes interrupt | user/manual + explicit-copy spec | P0 |
| 3 | State | Bare Ctrl+C without selection remains Pi interrupt; overlay/focus/ordinary input retires stale selection authority | prior spec R6 | P0 |
| 4 | Security | Mouse-only selection and arbitrary model/tool OSC52 output cannot write native clipboard or claim success | prior spec R5/C1 | P0 |
| 5 | Delivery | Reconciled managed extension includes clipboard adapter and OpenAI-only transport together; CLI and desktop both load it | observed version skew | P0 |
| 6 | Native boundary | macOS uses absolute osascript + explicit UTF-8/plain pasteboard type; Windows uses absolute PowerShell + strict UTF-8 + direct WinForms load; payload only via stdin | prior spec R1/R5/R8 | P0 |
| 7 | Failure/state | Success follows native completion; failure/timeout/cancel/reload suppress late success and permit recovery | prior spec R2/R4/R7 | P1 |
| 8 | Ordering/resource | Snapshot selection, FIFO writes, max 8 waiting, max 8 MiB UTF-8, reject NUL/overflow | prior spec R3 | P1 |
| 9 | Compatibility | SSH, unsupported OS/Pi, malformed private TUI shape fail passive without breaking providers | prior spec R7 | P1 |
| 10 | Acceptance | Isolated real macOS/Windows clipboard readback covers literal RTF/EPS and two Unicode markers | prior spec R8 | P2 |

## Spec coverage

- Explicit copy only: #2, #4
- CLI and desktop delivery: #1, #2, #5
- Interrupt/keybinding preservation: #3
- Trust boundary and redaction: #4, #6
- Lifecycle/concurrency/resource bounds: #7, #8
- Compatibility/fail-passive: #9
- Native end-to-end: #10

## RED proof

Import the independently authored final tests/fixtures from `origin/work/pi-editor-keys` into a clean branch based on `origin/main` (`5ebb6b0`) and commit them without implementation. Run targeted Vitest: expected failure is absence of `installFullscreenClipboard` and Mac copy-only routing on the OpenAI-only source.

## Mutation targets

1. Bypass explicit-key gate / restore mouse autocopy.
2. Change Ctrl+C consumption with no selection.
3. Remove macOS platform guard around super+c.
4. Write OSC52 from arbitrary terminal output.
5. Signal success before native completion.
6. Remove NUL/size/queue/timeout bounds.
7. Run queued write before prior child closes.
8. Remove lifecycle disposal/ownership checks.
9. Omit production lifecycle registration.
10. Reintroduce DeepSeek provider/model paths while porting old source.
